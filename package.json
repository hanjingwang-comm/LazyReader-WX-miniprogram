{
  "name": "layreader-wx-miniprogram",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A personal reading knowledge library with playful check-ins and optional cloud OCR",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit",
    "check": "npm run test && npm run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
